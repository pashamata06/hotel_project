services:
  - type: web
    name: hotel-project
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn hotel_project.wsgi:application
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
